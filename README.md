# TStoLowerCase
## toLowerCase
toLowerCase(input: string)

type ext = "C"

const aa: ext = "C"

const bb = toLowerCase(aa)

→typeof bb = "c"

## toUpperCase
toUpperCase(input: string)

type ext = "h"

const aa: ext = "h"

const bb = toUpperCase(aa)

→typeof bb = "H"
